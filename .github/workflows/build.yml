name: Android Build Placeholder

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Additional Android build files are still required before APK generation can succeed."